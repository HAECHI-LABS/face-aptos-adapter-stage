{
  "name": "face-aptos-adapter",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "bootstrap": "pnpm install && lerna exec pnpm build",
    "dev": "FORCE_COLOR=1 turbo run dev --filter=@haechi-labs/face-aptos-adapter-sample-dapp",
    "publish:minor": "pnpm run bootstrap && lerna version minor && lerna publish from-git",
    "publish:patch": "pnpm run bootstrap && lerna version patch && lerna publish from-git",
    "publish:prerelease": "pnpm run bootstrap && lerna version prerelease && lerna publish from-git",
    "fmt": "prettier --write '**/*.{js,jsx,ts,tsx}'",
    "check": "prettier --check '**/*.{js,jsx,ts,tsx}'",
    "lint": "eslint --fix '**/*.{js,jsx,ts,tsx}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.59.9",
    "@typescript-eslint/parser": "^5.59.9",
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-chai-expect": "^3.0.0",
    "eslint-plugin-eslint-plugin": "^5.0.2",
    "eslint-plugin-jest": "^26.5.3",
    "eslint-plugin-prettier": "^4.2.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-simple-import-sort": "^7.0.0",
    "prettier": "^2.8.8",
    "turbo": "^1.5.6",
    "typescript": "^5.1.3"
  }
}
